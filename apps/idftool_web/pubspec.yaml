name: idftool_web
description: Browser UI for idftool-style operations on ESP-IDF devices over Web Serial.
publish_to: none
version: 0.2.0+2
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  archive: ^4.0.7
  esp_monitor: ^0.1.0
  esptool: ^0.3.0
  file_picker: ^12.1.1
  http: ^1.2.0
  idftool: ^0.1.0
  share_plus: ^13.3.0
  web: ^1.1.0
  desktop_drop: ^0.8.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  fonts:
    - family: RobotoMono
      fonts:
        - asset: assets/fonts/RobotoMono-Regular.ttf
